---
description: Compatibility notice for the retired SDLC build workflow.
---

The standards library no longer owns build orchestration. In a Spec Kit project,
use the active integration's `speckit.implement` command. Outside Spec Kit, use
the project's documented build command. Apply the selected standards without
creating a parallel SDLC workflow.
