---
description: Compatibility notice for the retired SDLC discovery workflow.
---

The standards library no longer owns discovery sessions. In a Spec Kit project,
start or amend a specification with the active integration's
`speckit.specify` command and use `speckit.clarify` for material ambiguity.
Do not create a parallel discovery artefact.
