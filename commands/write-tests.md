---
description: Compatibility notice for the retired SDLC test-writing command.
---

The standards library no longer owns a test-writing phase. Define test work in
the Spec Kit plan and tasks, apply `~/.agents/sdlc/TESTING.md`, and use the
project's test interface. Use the `audit-tests` skill for an independent
findings-only review.
