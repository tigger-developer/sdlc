#+TITLE: W004 - Support Claude, Copilot, and Hermes Harnesses - Audit Evidence
#+STARTUP: content
#+OPTIONS: toc:2 num:nil

* Definition gate

** Local review - round 1
:PROPERTIES:
:ROUND:    1
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* SHA-256
  =da5d0c61439c9455c7c96564c12d1cd877d8db77074615cde479831680aef095=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions=
- *Verdict:* PASS
- *Findings:* None. The specification, solution design, and RT/OT definitions
  meet their focused audit contracts in the authoring context.
- *Remediation:* Clarified initializer routing, made the Codex compatibility
  test reproducible, prohibited external execution before local PASS, and
  recorded rejected designs.

** External review - round 1
:PROPERTIES:
:CONTEXT:  /root/audit_w004_definition
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* Git commit =68299b943e3d26ff6d3953e60b7bb1e5a9f02794=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions= in one
  context
- *Verdict:* FAIL
- *Findings:*
  1. DEF-W004-001 incorrectly compared the recorded file SHA-256 with Git's blob
     SHA-1. Recalculation from the committed blob confirms the recorded SHA-256
     is exact; no artefact correction was required.
  2. DEF-W004-002, DEF-W004-003, DEF-W004-006, and DEF-W004-009 required an
     exact per-harness capability, invocation, response, and resume contract.
  3. DEF-W004-004 and DEF-W004-010 required a concrete immutable audit boundary
     and adversarial verification.
  4. DEF-W004-005 required initializer mutation, recovery, and unsupported-
     capability boundaries.
  5. DEF-W004-007 required acceptance coverage for workflow instructions and
     public documentation.
  6. DEF-W004-008 required the validation ledger named by OT004.1 and OT004.2.
  7. DEF-W004-011 required an observable Codex compatibility baseline.
  8. DEF-W004-012 required all material edge cases in the opening summary.
  9. DEF-W004-013 required a clearer semantic emphasis spine.
- *Supersedes:* None.

** Local review - round 2
:PROPERTIES:
:ROUND:    2
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* SHA-256
  =df44991763f6cb35e9b9e023bfd3847265f07aadc732e483e14fd9e14e61513c=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions=
- *Verdict:* PASS
- *Findings:* None after rechecking the external findings against the revised
  specification.
- *Remediation:* Added the capability matrix, immutable audit bundle, response
  and incident grammar, initializer mutation boundary, documentation AC and UT,
  validation ledger, adversarial isolation check, exact Codex baseline, complete
  summary edge cases, and semantic emphasis.

** External review - round 2
:PROPERTIES:
:CONTEXT:  /root/audit_w004_definition
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* Git commit =c45c518342fcdd62cd4f6797399e215fd97589c3=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions= in one
  retained context
- *Verdict:* FAIL
- *Findings:*
  1. DEF-W004-008 required =PENDING= validation records with planned procedures
     and expected results.
  2. DEF-W004-014 required deterministic ownership treatment for old provider
     skill paths.
  3. DEF-W004-015 required one decision for an absent provider home.
  4. DEF-W004-009 required RT004.3 to induce the changed bundle it expects to
     detect.
  5. DEF-W004-016 required absent-home and legacy-copy installer cases.
  6. DEF-W004-013 required semantic emphasis in remaining scope and test
     definition material.
- *Supersedes:* External review round 1.

** Local review - round 3
:PROPERTIES:
:ROUND:    3
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* SHA-256
  =a1b762e84ab0834dfc02514740192acbd574f8bf0de971475ba9de5f2cc990d9=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions=
- *Verdict:* PASS
- *Findings:* None after rechecking the retained auditor's second-round
  findings.
- *Remediation:* Completed planned validation records; made native executable
  detection and skill-conflict confirmation deterministic; added absent-home,
  legacy-copy, confined-mutation, and bundle-mutation cases; and completed the
  semantic emphasis spine.

** External review - round 3
:PROPERTIES:
:CONTEXT:  /root/audit_w004_definition
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* Git commit =96120ade81d67140d5c3ab6cb6dfb8907f7f77f3=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions= in one
  retained context
- *Verdict:* FAIL
- *Findings:*
  1. DEF-W004-013 required semantic emphasis in unchanged boundaries and test
     definitions.
  2. DEF-W004-017 required executable detection cases independent of provider
     home presence.
  3. DEF-W004-018 required observable fields and continuation evidence for the
     initializer's interactive fallback.
  4. DEF-W004-019 required deterministic backup location, ownership, restore
     procedure, and byte-level recovery evidence.
- *Supersedes:* External review round 2.

** Local review - round 4
:PROPERTIES:
:ROUND:    4
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* SHA-256
  =af58c14c5052dfb9c768bb0e1136745e016ab2cc9500e97cf513b6a827c24a68=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions=
- *Verdict:* PASS
- *Findings:* None after rechecking the retained auditor's third-round findings.
- *Remediation:* Completed the semantic spine; separated executable and home
  fixtures; defined every fallback-handoff field and resumption check; and
  specified adjacent backup naming, ownership, restoration, and byte-level
  evidence.

** External review - round 4
:PROPERTIES:
:CONTEXT:  /root/audit_w004_definition
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* Git commit =681ac58e92e0921bbf8b25d4ff0612fcdc280273=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions= in one
  retained context
- *Verdict:* FAIL
- *Findings:* DEF-W004-013 required semantic emphasis across the remaining user
  and one-off test definitions, edge cases, architecture records, and delivery
  handoff.
- *Supersedes:* External review round 3.

** Local review - round 5
:PROPERTIES:
:ROUND:    5
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* SHA-256
  =bcf5005a7d4f20b6198d1ad684688c13aef3345ae2d9bee61b8b6597ab9defad=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions=
- *Verdict:* PASS
- *Findings:* None after a complete semantic-spine pass over every substantive
  section.
- *Remediation:* Added selective functional emphasis to the remaining scope,
  authority, constraints, decisions, validation definitions, edge cases,
  interface records, state, failures, quality impacts, and handoff.

** External review - round 5
:PROPERTIES:
:CONTEXT:  /root/audit_w004_definition
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* Git commit =bb44b7d31287b6608b39b8da7b9469060e2f720e=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions= in one
  retained context
- *Verdict:* PROVISIONAL
- *Findings:* DEF-W004-013 was reclassified under the verdict contract as one
  mechanical condition: add selective semantic emphasis to the exact passages
  at then-lines 501-503, 523-526, and 585-591 without changing their functional
  wording, then verify each bold-only reading.
- *Supersedes:* External review round 4.

** Condition receipt - effective PASS

- *Condition source:* External review round 5 on commit =bb44b7d=.
- *Resulting specification:* SHA-256
  =61f96f56dcef971c72b3e5032ebc17011579e92039afaceec584568cfd2362e8=.
- *Check:* The three identified passages retain their wording and now have a
  functional bold-only reading: local audits remain first and consolidated;
  tool-free capability is proven or external audit is unavailable; and the
  initializer handoff names every field, exact resume command, interactive
  harness, global skill, unchanged pending stage, and deterministic validation.
- *Changes outside condition:* Only lifecycle recording: =DEFINITION_GATE= is
  =PASS= and the work item is =REVIEW=.
- *Effective verdict:* PASS

** Subsequent definition change

- *Change:* Added cross-harness command-guard requirements, regression evidence,
  installation design, and security boundaries after the operator identified
  the omission.
- *Current specification:* SHA-256
  =822e18383b9ff2258b047569b42f749a5c76ddbccdb4cec702cae0a1556bf9ac=.
- *Effect:* The preceding PASS applies only to its recorded specification hash.
  The current definition gate is =PENDING= until this substantive revision is
  reviewed.

** Command-guard amendment - local review round 1
:PROPERTIES:
:ROUND:    1
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* SHA-256
  =822e18383b9ff2258b047569b42f749a5c76ddbccdb4cec702cae0a1556bf9ac=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions=
- *Verdict:* PASS
- *Findings:* None. The amendment now defines the exact canonical restrictions,
  native provider registrations, absent configuration and conflict behaviour,
  fail-closed payload handling, configuration preservation, security boundary,
  delivery handoff, and non-metered regression evidence.
- *Supersedes:* The pending status recorded immediately above for this revision.

** Command-guard amendment - external review round 1
:PROPERTIES:
:CONTEXT:  /root/audit_w004_command_guard
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* Git commit =897d8a94cd91520bb89bb030bbea0bb4fa322687=
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions= in one
  retained context
- *Verdict:* FAIL
- *Findings:*
  1. The exact restriction contract conflicted with current parser defects: data
     arguments were over-blocked while option-bearing =sudo= wrappers bypassed
     prohibited commands.
  2. Provider hook ownership, conflict disposition, and the observable
     SDLC-readiness result were not deterministic.
  3. RT004.7 lacked per-harness payload and result contracts, invocation-count
     evidence, malformed-input cases, exhaustive restriction vectors, allowed
     neighbours, and option-bearing wrappers.
- *Supersedes:* The command-guard amendment local review round 1.

** Command-guard amendment - local review round 2
:PROPERTIES:
:ROUND:    2
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* SHA-256
  =98ca9faf58ccbea54eb0438ae2cfef9d34c11538201a7683c3be2df5ca2157a4=.
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions=
- *Verdict:* PASS
- *Findings:* None after testing the external findings against the complete
  definition.
- *Remediation:* Distinguished command positions from ordinary argument data;
  specified supported wrappers and positive and negative vectors; defined
  exact provider payloads and results; made ownership, conflict handling, and
  readiness reporting deterministic; and expanded RT004.7 to prove those
  requirements without invoking a real provider.
- *Supersedes:* Command-guard amendment external review round 1.

** Command-guard amendment - external review round 2
:PROPERTIES:
:CONTEXT:  /root/audit_w004_command_guard
:END:

- *Artefact:* [[file:spec.org][W004 - Support Claude, Copilot, and Hermes Harnesses]]
- *Revision:* Git commit =d53ad02ea10ea42d4deff3b574e3134cdbb24c45=.
- *Audits:* =audit-spec=, =audit-design=, and =audit-test-definitions= in one
  retained context
- *Verdict:* PASS
- *Findings:* None.
- *Supersedes:* Command-guard amendment external review round 1.
- *Lifecycle result:* The definition gate is =PASS=. Operator sign-off remains
  =PENDING=; implementation is not authorized.

* Implementation gate

- *Authorization:* Taḋg approved W004 for implementation on 2026-09-07 after
  reviewing the definition-gate PASS.
- *Specification:* SHA-256
  =5de03e3f8c9a5fc46866d86498624531e90bc374a989f388a948a582b6e6b392=.

** Local review - round 1
:PROPERTIES:
:ROUND:    1
:END:

- *Revision:* Git commit =78f00bf=
- *Audits:* =audit-test-code= and =audit-code=
- *Verdict:* FAIL
- *Findings:*
  1. [audit-code] [BLOCKING] An unknown option-bearing =sudo= wrapper could
     bypass command restrictions.
  2. [audit-code] [BLOCKING] A literal =.env= search pattern was blocked while
     an unrecognized native hook payload could pass without a decision.
  3. [audit-test-code] [BLOCKING] Regression evidence did not yet cover the
     complete cross-harness phase matrix, native payload boundaries, conflict
     preservation, canonical skill refresh, or generic start and resume
     failures.

** Local review - round 2
:PROPERTIES:
:ROUND:    2
:END:

- *Revision:* Git commit =8115b3c=
- *Audits:* =audit-test-code= and =audit-code=
- *Verdict:* FAIL
- *Findings:*
  1. [audit-code] [BLOCKING] Archived Spec Kit classification invoked a harness
     outside the shared runner, bypassing the configured timeout, process-group
     termination, bundle verification, and typed incident contract.
  2. [audit-test-code] [BLOCKING] The installer verifier required an executable
     command guard, but the tracked guard was mode =0644=; a real installation
     therefore failed after writing its projections.
- *Remediation:* Routed classification through the shared harness runner and
  restored executable mode on the canonical guard. The installer regression
  now exercises that mode contract.

** Local review - round 3
:PROPERTIES:
:ROUND:    3
:END:

- *Revision:* Git commit
  =a148c28409c007e1bcdff4e0c2a7af5190cd9584=
- *Audits:* =audit-test-code= and =audit-code=
- *Verdict:* PASS
- *Findings:* No required correction remains.
- *Evidence:* =make test= passed Go vet, golangci-lint, ShellCheck, shfmt, and
  all Go regression packages on 2026-09-08. Focused project-initializer,
  harness-runner, runner-CLI, and uncached installer regressions also passed.
- *Advisory:* The canonical shell guard is now 432 lines, above the shell
  complexity tripwire. Its behaviour is covered by focused regression tests,
  but a later change should consider moving parsing into a small structured
  implementation without weakening the single canonical policy.
- *Status:* Local implementation gate PASS. One retained external implementation
  audit remains required.

** External review preflight incident 2

- *Attempted revision:* Git commit =fc088d8=
- *Result:* Codex stopped before creating an external context or making a
  metered model call.
- *Incident:* The fixed Codex adapter changed into the immutable temporary
  bundle but omitted =--skip-git-repo-check=, so Codex rejected the deliberately
  non-repository directory.
- *Effect:* The local round 4 PASS remains revision-specific but is no longer
  current after remediation.

** Local review - round 5
:PROPERTIES:
:ROUND:    5
:END:

- *Revision:* Git commit =9d23b40=
- *Audits:* =audit-test-code= and =audit-code=
- *Verdict:* PASS
- *Findings:* No required correction remains.
- *Remediation:* The Codex start and resume vectors now use the documented
  =--skip-git-repo-check= option while retaining the read-only sandbox and
  immutable bundle. Definition remains on the operator-selected branch;
  implementation branching is now owned explicitly by =deliver-change=.
- *Evidence:* =make test= passed all static checks and Go regression packages on
  2026-09-08.
- *Status:* Local implementation gate PASS at the five-round limit. One retained
  external implementation audit remains required.

** External review - round 1
:PROPERTIES:
:CONTEXT:  01a07e47-4e3b-7ff0-a7fb-835f802413e8
:END:

- *Revision:* Git commit
  =4933cb5eda8483b1ea737f76a4af635eee3b7ade=
- *Audits:* =audit-test-code= and =audit-code= in one retained context
- *Verdict:* FAIL
- *Findings:*
  1. [audit-code] [BLOCKING] AC004.3, AC004.4, AC004.5, AC004.11, RT004.3,
     and RT004.7: installer readiness reports more than its one guard probe
     proves; the guard also mishandles =.env= used as data and an unknown tool
     payload.
  2. [audit-code] [BLOCKING] AC004.6 and RT004.1: an existing non-symlink skill
     path lacks managed-ownership evidence and a distinct unknown-conflict
     decision before replacement.
  3. [audit-code] [BLOCKING] AC004.7 and RT004.4: uppercase =--phase AUDIT= can
     bypass composite-verdict validation.
  4. [audit-test-code] [BLOCKING] AC004.4, AC004.7, RT004.3, RT004.4, and
     RT004.5: fake-executable start and resume coverage does not exercise the
     complete four-harness identity, timeout, exit, and response-failure matrix.
  5. [audit-test-code] [BLOCKING] AC004.6, AC004.11, RT004.1, and RT004.7:
     installer coverage does not fully prove installed native payloads,
     invocation counts, unrelated-byte preservation, restoration, conflict
     decline, and all-provider no-op behaviour.
  6. [audit-code] [BLOCKING] AC004.7, AC004.9, and Go error handling: bundle
     cleanup and configuration-backup restoration errors are discarded.
  7. [audit-code] [BLOCKING] AC004.10: =docs/ARCHITECTURE.md= still says v3
     installs no workflow into Claude, Hermes, or Copilot.
- *Disposition:* The five local rounds are exhausted. No remediation has been
  attempted after this verdict; operator direction is required.

** External review preflight incident

- *Attempted revision:* Git commit
  =a148c28409c007e1bcdff4e0c2a7af5190cd9584=
- *Result:* No external context or metered model call was created.
- *Incident:* The runner rejected a provider inherited from the global audit
  configuration even though the selected Codex adapter does not accept that
  parameter. W004 requires inherited irrelevant provider values to be ignored.
- *Effect:* The local round 3 PASS remains revision-specific but is no longer
  current after remediation.

** Local review - round 4
:PROPERTIES:
:ROUND:    4
:END:

- *Revision:* Git commit =18e4669=
- *Audits:* =audit-test-code= and =audit-code=
- *Verdict:* PASS
- *Findings:* No required correction remains.
- *Remediation:* Configuration now ignores inherited provider values for Codex,
  Claude, and Copilot while continuing to reject a directly supplied
  conflicting runner override. A regression covers the global Codex provider
  configuration that exposed the defect.
- *Evidence:* =make test= passed all static checks and Go regression packages on
  2026-09-08.
- *Status:* Local implementation gate PASS. One retained external implementation
  audit remains required.

** Operator-directed remediation of external review round 1

- *Revision:* Git commit =d23ed9c=.
- *Scope:* The seven findings from [[*External review - round 1][external review round 1]]. This is a
  remediation receipt, not a sixth local audit or a replacement external
  verdict.
- *Disposition:*
  1. Installer verification now checks delivery through every detected native
     guard and validates every detected start and resume adapter before reporting
     readiness. The guard distinguishes =.env= used as ordinary output data from
     a file read, preserves parser state across nested command strings, and
     blocks an unknown native tool whose payload targets =.env=.
  2. The operator rejected the skill-path ownership finding. Accepted
     replacement remains recoverable through the installer's adjacent backup;
     no additional ownership mechanism was added.
  3. Phase names are normalized before configuration resolution and audit
     verdict validation, so =--phase AUDIT= cannot bypass the audit contract.
  4. Persistent fake-executable regressions now cover start, resume, timeout,
     non-zero exit, empty response, identity, and response parsing across all
     four harnesses. Hermes additionally rejects a missing or duplicated native
     session envelope.
  5. Installer regression now proves four native guard invocations, unrelated
     byte preservation, accepted backup content, declined-conflict preservation,
     canonical skill refresh, and an all-provider no-op rerun.
  6. Migrated-work bundle cleanup errors and configuration-backup restoration
     errors are returned instead of discarded. A regression proves that both
     the original configuration-write error and restoration error remain
     inspectable.
  7. =docs/ARCHITECTURE.md= now describes the canonical multi-harness skill,
     guard, and audit-adapter architecture.
- *Evidence:* =make test= passed Go vet, golangci-lint, ShellCheck, shfmt, and all
  Go regression packages on 2026-09-08. =sanitize= produced no changes for the
  amended Markdown documentation, and =git diff --check= passed before the
  implementation commit.
- *Audit status:* No further external audit was run, following the operator's
  direction to avoid another external audit loop. The preceding external FAIL
  remains the last independent verdict; operator sign-off and the pending UT/OT
  evidence remain separate decisions.
