#+TITLE: W014 - Installer Trash validation
#+STARTUP: overview

* 2026-09-13 - Local regression evidence

- RED :: SDLC replacement test observed an adjacent backup; missing, failing
  and no-move trash tests observed replacement proceeding. Companion deployer
  drift test likewise found an adjacent backup instead of recovered Trash bytes.
- GREEN :: Installer package and companion deployer regression suite passed
  after switching managed replacement to Trash and preserving config backups.
- Boundaries :: All trash invocations use a local command double moving exact
  fixture paths into a disposable recovery tree. No host Trash or deployment used.
- Existing evidence :: Installer integration covers retired commands, symlink
  recovery, provider configuration preservation and unchanged reruns.
- Static checks :: =go vet= and =golangci-lint= passed for the companion deployer
  and the changed SDLC installer packages. =gofmt= and =git diff --check= are clean.
- Documentation :: Changed documentation passed =sanitize= without differences.

* Operator validation

- Pending :: Run installation on the operator's host and confirm Trash recovery
  and adjacent configuration backups. No live installation was run by this agent.

* 2026-09-13 - Emergency audit-fallback follow-up

- RED :: New process-boundary tests reproduced no fallback for subscription
  limits, launch failures, timeout and unusable responses.
- GREEN :: =go test ./cmd/sdlc-harness ./internal/harness= passed after the
  recovery change. Valid verdicts avoid fallback; failed fallback stops; budgets,
  evidence integrity, provenance and retained fallback reuse are checked.
- Fixture correction :: Two existing tests expected a provider invocation for
  an identical cached result. The mutation probe now changes its caller context;
  the timeout test accepts a cached PASS while verifying no extra provider call.
- Scope :: Local provider doubles only; no metered regression test.
- Response isolation :: A further RED probe demonstrated a failed primary's
  temporary-file PASS being accepted as fallback output. Clearing that exact
  temporary file before each attempt makes the fallback correctly fail empty.
