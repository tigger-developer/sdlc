#+TITLE: W007 - Isolated auditor validation
#+STARTUP: content

* OT007.1 - Live isolation probe

- Category :: One-off test.
- Covers :: AC007.1 - Isolated invocation; AC007.2 - Structured response.
- Status :: PASS for the bounded synthetic probe; not a general file-isolation claim.
- Bounds and expected result :: [[file:spec.org::#ot-007-1][Live isolation probe definition]].
- Attempt :: 2026-09-11, Claude Code 2.1.236, macOS; first prototype working tree.
- Observation :: Provider exited 1 with =is_error: true= and =Not logged in; Please run /login=. No structured audit result was returned. Wrapper rejected the result.
- Interpretation :: Authentication blocked the test before safe-mode isolation could be qualified. No credential extraction or fallback was attempted. Login in the scratch configuration is operator-owned; another live attempt requires the next paired instruction.

** 2026-09-11 - Retry after operator login

- Authorization :: Operator reported the scratch-profile login had been performed.
- Tested revision :: =20bb510= - initial paired prototype.
- Observation :: The retry again returned =is_error: true= and no structured result; the native diagnostic reported not logged in. Input and output token counts were both zero.
- Isolation check :: =claude auth status= using the same scratch configuration returned =loggedIn: false= and =authMethod: none= both with and without =--safe-mode=. No model calls were made by these status checks.
- Next diagnostic :: Compare authentication status from the operator terminal using that exact configuration directory. The cause of the unavailable login is not yet established; repeating a model call would add no useful evidence.

** 2026-09-11 - Authentication discrepancy resolved and probe executed

- Cause established :: The operator's pasted configuration path contained a literal newline and spaces. Auth status reported logged in for that different path, but not for the prototype path. After login to the intended path, auth status reported =loggedIn: true= and =authMethod: claude.ai=.
- Tested revision :: =9934c9a= - authentication retry checkpoint; prototype code unchanged from =20bb510=.
- Environment :: Claude Code 2.1.236 on macOS, =--safe-mode=, =--tools ""=, isolated scratch configuration, requested model =sonnet=.
- Observation :: In 2,970 ms of native reported duration, the synthetic defective implementation received the expected rejection. The wrapper accepted the structured response and native identity. This is fixture evidence, not an audit verdict on W007.
- Instruction probe :: Neither synthetic user nor project instruction marker appeared in the native transcript or final response. This is bounded contamination evidence, not proof that every implicit context source is suppressed.
- Tools observed :: Only the internal =StructuredOutput= tool appeared in the transcript. No shell, file, network or delegation tool calls appeared. The phrase tool-free means no operational tools; structured response delivery remains enabled.
- Model observation :: Native usage reports =claude-sonnet-5= for review and an additional small =claude-haiku-4-5-20251001= usage entry on the initial invocation. This was not exclusively Sonnet execution. Cost estimates in the native envelope are not evidence of an additional subscription charge.
- Evidence retention :: Raw JSON responses and the native transcript remain in the probe's scratch directory for paired inspection. No production audit record was written.

* OT007.2 - Live retained-context probe

- Category :: One-off test.
- Covers :: AC007.3 - Same-session continuation.
- Status :: PASS for the bounded synthetic start/resume sequence.
- Bounds and expected result :: [[file:spec.org::#ot-007-2][Live retained-context probe definition]].
- Earlier observation :: Initially not invoked because the startup probe did not produce a successful audit session.
- Execution :: 2026-09-11, same environment and tested revision as the successful startup probe. One resume invocation supplied only the corrected implementation and the original test reference, not the requirement.
- Observation :: Native response in 2,524 ms accepted the corrected fixture with no findings. The wrapper verified that the returned session identity exactly matched the initial native identity. Native Sonnet usage reported 1,239 cache-read input tokens.
- Limitation :: This proves retained context for this tiny example, not performance or recall on a large project. Timeout/process termination and original-file access were not exercised live.

* RT007.1 - Local response validation

- Category :: Automated regression test; no external service.
- Result :: PASS with =go test -race ./prototype/claude-audit=.
- RED :: Before implementation, compilation failed because =parseResult= did not exist.
- GREEN :: Start/resume identities, provider errors, missing structured results, invalid verdicts and empty findings are checked locally.
- Additional checks :: =go vet= and =golangci-lint= passed for the prototype package. These checks do not qualify Claude's live isolation.

* OT007.3 - Native file-permission qualification

- Category :: One-off test; no persistent metered test target.
- Status :: PASS for the three explicitly attempted synthetic paths; not a universal sandbox qualification.
- Procedure and expected result :: [[file:spec.org::#ot-007-3][Native file-permission qualification]].
- Tested baseline :: =cfa90c7= - paired start/resume evidence; native Claude flags varied for this experiment only. Production harness and compiled prototype remain unchanged.
- Environment :: 2026-09-11, macOS, Claude Code 2.1.236, subscription login in the existing isolated scratch profile, requested model =sonnet=.
- Invocation :: Direct native CLI with safe mode, =Read= as the only operational tool, =dontAsk=, explicit exact-path allow rules, no additional directories, no ordinary settings sources or MCP servers. Explicit denies cover the empty working directory, scratch authentication state and user directories. No explicit deny names the unlisted fixture or symlink target.
- Observation :: One successful native result in 6,183 ms. The permitted original file returned its expected text. Both the unlisted sibling and the explicitly allowed symlink spelling produced native permission denials; the symlink target had no grant.
- Verification :: Native transcript contains exactly three =Read= calls and one =StructuredOutput= call. The two denied reads have error tool results and matching native =permission_denials= entries. The unlisted fixture marker appears in neither tool results nor the transcript. This tests enforcement rather than voluntary prompt compliance.
- Turn accounting :: The CLI was passed =--max-turns 4= but reported =num_turns: 5=. No additional read attempts occurred. The two-minute external process timeout remains the elapsed-runtime bound; the meanings of native turn counters are not qualified by this observation.
- Retention :: Synthetic fixtures, settings, prompt, schema and raw result remain in the operating-system scratch directory whose generated basename is =sdlc-claude-read-probe.OOQ3vrx0ZZ=. Native session state remains in the existing isolated authentication profile. No credentials were inspected or copied.
- Limitation :: This is not an OS sandbox or proof against every implicit readable path, managed-policy variation, path race, or future CLI change. Session resume was not tested in this first invocation; the subsequent OT007.4 entry records that separate check. Production changed-file evidence integration remains untested. No production =audits.yaml= was written.

* OT007.4 - Resumed original-file reads

- Category :: One-off test.
- Status :: PASS for this synthetic retained-session sequence.
- Procedure and expected result :: [[file:spec.org::#ot-007-4][Resumed original-file reads]].
- Tested baseline :: =b115f54= - native file-permission probe checkpoint; compiled prototype and production harness unchanged.
- Environment :: 2026-09-11, macOS, Claude Code 2.1.236, requested model =sonnet=, same isolated configuration, working directory and permissions as OT007.3.
- Execution :: One native =--resume= invocation using the identity returned by the earlier file-permission probe. The input referred to the same paths from retained context and reported that the permitted file had changed; it did not supply paths again or reveal the new content. External timeout remained two minutes with two-second kill escalation.
- Observation :: Successful structured response in 6,087 ms, with the same native session identity. The permitted file returned the replacement fixture text rather than its original contents. Both the unlisted sibling and symlink returned native permission denials.
- Verification :: Inspected the transcript records appended after the 17 pre-existing records. Exactly three new =Read= calls and one =StructuredOutput= call appeared. Actual tool results contain the replacement text and two denial errors; the native response has matching denial entries. The unlisted marker is absent from the appended transcript.
- Turn accounting :: As in OT007.3, =--max-turns 4= produced native =num_turns: 5= with no extra read attempts; exact native counter semantics remain unqualified.
- Retention :: The same synthetic scratch directory retains =resume-input.txt= and =resume-result.json=. The permitted fixture now contains the replacement text; the original tool result remains in the initial response and transcript. No credentials, production audit records or installed configuration were edited.
- Limitation :: Reusing a session with explicitly supplied unchanged permissions works for these fixtures. This does not prove permission revocation, arbitrary path isolation, realistic audit quality or automatic changed-file detection in the production harness.

* OT007.5 - Change-scoped real harness review

- Category :: One-off qualification test, not the W006 implementation gate.
- Status :: PENDING.
- Procedure and expected result :: [[file:spec.org::#ot-007-5][Change-scoped real harness review]].
- Tested revision :: =38e5e95= - retained-read checkpoint. Git comparison confirms the W006 implementation inputs are unchanged from =e9a1bd7=.
- Environment :: 2026-09-11, macOS, Claude Code 2.1.236, requested model =sonnet=, isolated subscription profile, safe mode, Read only, exact-path permissions and =dontAsk=.
- Observation :: The five-minute external timeout ended the single invocation with exit 124. The raw final-response file is empty and the native transcript contains no structured-response tool call. This is an interrupted qualification, not a PASS or FAIL verdict on the harness code.
- Boundary evidence :: The transcript records 13 reads, all within the explicit allowlist, with no permission errors or other tool calls. Reads continued during the invocation, including the selected standards and changed prompt/session-output code. Every one of the 22 original evidence-file hashes remained unchanged after termination. The timeout and Claude process IDs were absent on the post-run process check.
- Unqualified :: No findings were returned, so finding quality and useful review completion remain unassessed. Restricted access alone did not produce a result within five minutes; this observation does not isolate the cause of the runtime.
- Retention :: The native session and synthetic runtime profile remain available. The scratch directory with generated basename =sdlc-claude-harness-review.yC8YmqHC7c= retains the prompt, schema, allowlist, change diff, input hashes and timeout observation. No automatic second call, remediation or canonical audit-record write occurred.

* Operator validations

No operational validation or closure has yet been given.
