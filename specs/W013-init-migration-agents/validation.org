#+TITLE: W013 - Migration-agent validation
#+STARTUP: overview

* 2026-09-12 - Local regression evidence
- RED :: Both new coordinator tests failed with the existing Hermes refusal;
  the native command was never reached.
- GREEN :: =go test ./internal/projectinit -count=1= passed after implementation.
- Static checks :: =go vet ./internal/projectinit= passed. =cmd/sdlc-init= built
  into the disposable verification directory without installing it.
- Scope :: Local temporary projects and command doubles only; no metered API,
  real ticket migration, client-project mutation or installation.
- Repair :: Existing canonical skip and invalid-output rejection tests passed;
  updated repair test uses a definition model distinct from the audit model.
- Resume :: Full v1 initialization test now fails its first migration launch,
  resumes the same migration, and verifies consolidated requirements and archive.
- Fixture maintenance :: Updated the v2 classification double to emit Hermes'
  native =session_id:= on stderr, matching the existing adapter contract.

* Native integration
- Operator migration retry after installation is not yet observed. These local
  results do not establish model compliance, authentication or migration quality.

* 2026-09-12 - Paired definition-profile and resume amendment
- RED :: Updated routing test observed Hermes from the audit profile instead
  of Codex from the definition profile. Resume tests failed for advanced primary
  tips and for master/main invocation before the implementation change.
- GREEN :: =go test ./internal/projectinit -count=1= passed after the amendment.
- Recovery cases :: Already on migration branch; y/yes confirmation from
  master/main; n or Enter declines; ambiguous candidates, unrelated archive and
  conflicting edits stop without switching or removing recovery state.
- Native boundary :: All recovery tests use real Git in disposable local repos.
  Provider routing uses command doubles, with no live migration or metered RT.
- User validation :: No new operator validation has yet been recorded.

* 2026-09-12 - Paired legacy VALID terminology
- RED :: Parser tests rejected VALID and returned HOLD for old generated labels;
  consolidation failed the new VALID headline/type-declaration expectations.
- GREEN :: =go test ./internal/projectinit -count=1= passed after the change.
- Preservation :: Real local ledger consolidation preserves PENDING test
  evidence, qualifications, references and a BLOCKED normal work item. Existing
  rerun checks establish idempotence after the source AC ledger is removed.
- Boundary :: Local fixtures only. Existing project ledgers remain operator-owned
  manual updates; no live migration or metered regression check was performed.
