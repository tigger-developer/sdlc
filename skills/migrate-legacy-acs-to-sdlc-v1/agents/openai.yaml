interface:
  display_name: "Retire SDLC v1 tickets"
  short_description: "Archive legacy issues and close the old tracker"
  default_prompt: "Use $migrate-legacy-acs-to-sdlc-v1 to archive and close this brownfield project's legacy tickets before Spec Kit initialization."

policy:
  allow_implicit_invocation: false
