## SDLC standards

The canonical SDLC root is exactly `~/.agents/sdlc`. Never search the
filesystem to locate it. If `~/.agents/sdlc/MAIN.md` is absent or unreadable,
report that exact path.

Read `~/.agents/sdlc/MAIN.md` and the documents listed in the constitution's
Engineering Standards Profile that apply to the current tasks. Do not preload
unselected language or domain documents. Implement only defined specification
behaviour, preserve human edits, and report verification precisely.
